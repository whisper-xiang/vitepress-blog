{
  "cleanUrls": true,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "pnpm store prune && pnpm config set registry https://registry.npmjs.org/ && pnpm install --no-frozen-lockfile"
      }
    }
  ]
}
